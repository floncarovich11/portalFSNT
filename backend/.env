# Variáveis de ambiente do backend
NODE_ENV=development

# Segredo usado para assinar/verificar JWT — troque por um valor forte em produção
JWT_SECRET=troque_por_um_segredo_muito_forte_aqui

# Tempo de expiração padrão do token
JWT_EXPIRES_IN=8h
